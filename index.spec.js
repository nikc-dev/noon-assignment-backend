

describe('Test Suite that will test the APIs for the App', () => {
    it('should fail', () => {
      expect(false).toEqual(true);
    });

    it('should pass', () => {
        expect(true).toEqual(true);
      });
  });